<template name="img">
  <image class="{{item.classStr}} wxParse-{{item.tag}} live-detail__img" 
    data-from="{{item.from}}" 
    data-src="{{item.attr.src}}" 
    data-idx="{{item.imgIndex}}" 
    src="{{item.attr.src}}" 
    bindtap="wxParseImgTap" 
    mode="widthFix"
  />
</template>